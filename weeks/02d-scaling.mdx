---
title: "Scaling"
week: 2
order: 3
---

import powerLaw from "./assets/week02-power-law.png";
import isoFlop from "./assets/week02-isoflop.png";
import emergenceMetric from "./assets/week02-emergence-metric.png";
import emergenceObserved from "./assets/week02-emergence-observed.png";

Pre-training에서는 문서를 고르고 학습 시퀀스를 만든 뒤, 정답 토큰의 평균 음의 로그 확률을 줄이도록 모델을 학습했다. 그렇다면 **모델을 더 크게 만들고, 더 많은 데이터와 계산량을 사용하면 성능도 계속 좋아질까? 얼마나 더 좋아질지 미리 알 수 있을까?** Li 등의 *(Mis)Fitting: A Survey of Scaling Laws*를 바탕으로, 규모와 성능 사이의 관계를 살펴본다.[^survey] 먼저 규모를 키울 때 loss가 줄어드는 경향을 보고, 그 관계를 이용한 계산 예산 배분으로 넘어간다. 마지막에는 평균 loss의 개선이 개별 과제의 능력 향상으로 어떻게 나타나는지 구분한다.

## 1. Scaling laws

어떤 모델을 학습해 성능을 측정했다고 하자. 모델 크기를 두 배로 늘리면 얼마나 나아질까? 학습 데이터를 두 배로 늘리면 어떨까? 계산량을 열 배 투입할 가치가 있을까? 작은 규모에서 얻은 개선이 더 큰 규모에서도 이어지는지, 아니면 어느 지점부터 다른 제약에 막히는지가 궁금해진다.

Scaling law는 이런 질문에 답하기 위해 **모델·데이터·계산 규모와 성능 사이의 관계를 실험으로 측정하고 수식으로 표현**한다. 이 노트에서는 먼저 성능을 평가 데이터의 평균 next-token loss로 측정한다. Loss가 얼마나 줄어드는지와, 질의응답·추론 같은 개별 과제의 점수가 얼마나 오르는지는 구분해서 살펴봐야 한다.

작은 실험에서 얻은 관계가 더 큰 실험에서도 맞는다면, 아직 실행하지 않은 규모의 성능을 예측할 근거가 생긴다. 여기서 law는 주어진 실험 조건에서 관찰하고 맞춘 경험적 관계다. 규모를 무한히 키우면 성능도 계속 좋아진다는 보장이나, 데이터 분포와 모델 구조가 바뀌어도 같은 계수가 유지된다는 자연법칙은 아니다.

### 1-1. 모델·데이터·계산량

기호부터 구분하자.

| 기호 | 뜻 | 이 노트의 단위 |
|---|---|---|
| $N$ | 모델의 파라미터 수 | parameters |
| $D$ | 학습 중 소비한 토큰 수 | tokens |
| $C$ | 학습의 계산량 | FLOPs(floating-point operations) |
| $\mathcal{L}$ | 평가 분포의 평균 next-token loss | nats/token |

$D$는 고유한 텍스트 양과 같지 않을 수 있다. 같은 데이터를 반복해서 읽어도 소비한 토큰 수는 늘어난다. 또한 tokenizer가 바뀌면 같은 문서의 토큰 수가 달라지므로 서로 다른 실험의 $D$를 해석할 때 조건을 확인해야 한다.

$\mathcal L$은 Pre-training에서 본 평균 NTP(next-token prediction) loss를 평가 데이터에서 계산한 값이다. 자연로그를 사용하므로 단위가 nats/token이며, perplexity는 $\exp(\mathcal L)$이다. 모델끼리 비교할 때는 tokenizer·평가 텍스트·제공 문맥·채점 위치를 맞춘다. 아래에서는 이러한 조건과 모델 계열을 고정하고 $N,D$를 바꾼다고 생각하자.

Dense Transformer의 학습 계산량을 거칠게 추산할 때 다음 근사(approximation)를 사용한다.[^chinchilla]

$$
C\approx6ND
$$

계수 6은 앞에서 본 **행렬곱의 forward와 backward 비용**에서 출발한다. 토큰 벡터 $x\in\mathbb R^{1\times d_{\mathrm{in}}}$에 가중치 $W\in\mathbb R^{d_{\mathrm{in}}\times d_{\mathrm{out}}}$를 곱해 $y=xW$를 만든다고 하자. 이 행렬의 파라미터 수는 $N_W=d_{\mathrm{in}}d_{\mathrm{out}}$다.

출력 성분 하나는 $d_{\mathrm{in}}$번 곱하고 그 결과를 더한다. 곱셈과 덧셈을 각각 한 연산으로 세면, forward 비용은 대략 $2d_{\mathrm{in}}d_{\mathrm{out}}=2N_W$다. Backward에서는 출력 gradient $g_y$로부터 입력 gradient $g_x=g_yW^\top$와 가중치 gradient에 대한 기여 $x^\top g_y$를 계산한다. 가중치 gradient는 여러 토큰의 기여를 더하므로 곱셈과 누적 덧셈을 함께 센다. 두 backward 계산을 각각 토큰당 약 $2N_W$로 세어, forward까지 총 $6N_W$로 근사한다.

| 계산 | 행렬곱 중심의 근사 비용 |
|---|---|
| Forward: 출력 계산 | $2N_W$ |
| Backward: 입력 gradient | $2N_W$ |
| Backward: 가중치 gradient | $2N_W$ |
| 합계 | $6N_W$ |

<Callout type="example">
입력 4차원, 출력 3차원의 작은 선형층은 가중치가 12개다. 출력 세 개를 구할 때 각각 4번 곱하고 3번 더하므로 정확한 forward 연산 수는 21회다. 큰 행렬에서는 덧셈 횟수를 곱셈 횟수와 같게 근사해 $2\times12=24$회로 센다. 같은 방식으로 backward까지 약 72회로 잡는다. 이 계산을 모델의 주요 가중치 행렬에 합산하고, 소비한 $D$개 토큰에 걸쳐 누적한 값이 $6ND$다.
</Callout>

이는 주된 행렬곱 중심의 근사다. 긴 시퀀스의 attention 계산, embedding 취급, activation 재계산 등까지 정확히 포함한 실행 비용은 아니다. MoE(mixture of experts)에서는 전체 파라미터를 그대로 대입해서도 안 된다. 또한 **FLOPs는 연산 횟수**이고 FLOP/s는 초당 연산 처리량이다. 같은 연산량도 하드웨어·통신·활용률에 따라 걸리는 시간이 달라진다.

### 1-2. Power law와 loss

먼저 여러 크기의 모델을 여러 학습 토큰 수로 훈련하고, 각 실험의 $(N,D,\mathcal L)$을 기록한다. 이 점들에서 규모가 커질수록 loss가 어떻게 줄어드는지 관찰한 뒤, 그 경향을 설명할 수식을 정하고 계수를 추정한다. 이렇게 관측값에 수식을 맞추는 과정을 **fitting**이라고 한다. 추정에 사용하지 않은 실험에서도 맞는지 확인해야 큰 규모의 예측에 쓸 근거가 생긴다.

**Kaplan et al. (2020).** 충분한 데이터로 수렴시킨 모델에서는 $\mathcal L(N)\propto N^{-\alpha_N}$, 충분히 큰 모델을 제한된 데이터로 학습하고 평가 loss에 따라 조기 종료한 경우에는 $\mathcal L(D)\propto D^{-\alpha_D}$를 관찰했다. 한 자원의 영향을 볼 때 다른 자원이 병목이 되지 않도록 한 실험이다.[^kaplan]

두 제약을 함께 표현한 원논문의 식 (1.5)는 다음과 같다. 여기서만 $D_{\mathrm{set}}$으로 표기를 바꾸어 **데이터셋 크기**임을 드러낸다.

$$
\widehat{\mathcal L}_{\mathrm K}(N,D_{\mathrm{set}})
=\left[\left(\frac{N_c}{N}\right)^{\frac{\alpha_N}{\alpha_D}}
+\frac{D_c}{D_{\mathrm{set}}}\right]^{\alpha_D}
$$

분자 $N_c,D_c$는 고정된 상수이고, 분모 $N,D_{\mathrm{set}}$이 커질수록 해당 항이 줄어든다. $(N_c/N)^{\alpha_N/\alpha_D}=(N/N_c)^{-\alpha_N/\alpha_D}$이므로 비율을 뒤집어 쓰려면 지수의 부호도 바꿔야 한다.

$N_c,D_c$는 척도 상수다. $D_{\mathrm{set}}$이 충분히 크면 $(N_c/N)^{\alpha_N}$, $N$이 충분히 크면 $(D_c/D_{\mathrm{set}})^{\alpha_D}$에 접근한다. Kaplan의 $N$은 embedding을 제외한 파라미터 수이며, 이 식의 데이터셋 크기는 반복 학습까지 센 소비 토큰 수와 구별한다. 원논문 Table 2의 **공동 fitting**은 $\alpha_N=0.076$, $\alpha_D=0.103$을 보고한다. 별도 단일 변수 실험의 $\alpha_D\approx0.095$를 이 계수와 섞어 쓰지 않는다.[^kaplan]

### 1-3. Hoffmann의 loss 모형

**Hoffmann et al. (2022), Chinchilla.** 모델 크기와 **학습 토큰 수**를 함께 바꾼 실험에 다음 가산 모형을 맞춘다. 이는 Kaplan의 괄호 전체에 지수를 취한 식과 다른 함수다.[^chinchilla]

$$
\widehat{\mathcal{L}}(N,D)=E+\frac{A}{N^\alpha}+\frac{B}{D^\beta}
$$

$A,B,\alpha,\beta$는 실험에 맞춰 추정하는 양수다. $E$는 이 모형이 추정하는 점근적 loss 항이다. **아래 그림과 미분 유도는 이 Hoffmann 모형을 기준으로 한다.** 식을 참으로 가정하는 대신, 관측한 실험을 얼마나 잘 설명하고 새로운 실험을 얼마나 잘 예측하는지 확인한다.

Power law는 규모를 일정 배수로 늘릴 때 감소 항이 일정 비율로 줄어드는 관계다. 예를 들어 $N^{-0.5}$는 $N$을 네 배로 늘릴 때 절반이 된다. 같은 양의 파라미터를 더했을 때 얻는 절대 loss 감소는 점점 작아진다. 고정된 데이터 조건에서는 다른 항이 남으므로, 전체 loss 자체가 무한히 0으로 향한다고 읽어서는 안 된다.

이 관계를 축으로 읽을 때는 **전체 loss와 감소 항을 구분**한다. $D$를 고정하고 $F=E+B/D^\beta$라고 놓으면 $\widehat{\mathcal L}-F=AN^{-\alpha}$다.

$$
\log(\widehat{\mathcal L}-F)=\log A-\alpha\log N
$$

따라서 가로축을 $\log N$, 세로축을 $\log(\widehat{\mathcal L}-F)$로 놓으면 이 모형은 기울기 $-\alpha$의 직선이다. 전체 loss를 그대로 로그로 바꾸면 바닥 항 $F$ 때문에 같은 직선이 되지 않는다. 아래 왼쪽 패널은 일반 축의 전체 loss, 오른쪽은 바닥 항을 뺀 뒤 양쪽 축을 로그 척도로 바꾼 결과다. 뒤의 가상 계수에서는 $N$을 1B→4B→16B로 네 배씩 늘리면, 바닥 2를 뺀 loss가 1→0.5→0.25로 절반씩 줄어든다.

$N$을 늘리면 첫 번째 감소 항이 작아지고, $D$를 늘리면 두 번째 감소 항이 작아진다. 둘 중 하나만 키우면 다른 항이 남는다. 예산이 고정되어 있다면 둘을 마음대로 동시에 늘릴 수 없으므로 낮은 loss를 주는 조합을 찾아야 한다.

<Figure src={powerLaw.src} alt="왼쪽 일반 축에서는 모델 크기 N이 1B, 4B, 16B일 때 전체 loss가 3, 2.5, 2.25로 바닥 2에 접근한다. 오른쪽 log–log 축에서는 바닥을 뺀 값 1, 0.5, 0.25가 기울기 -0.5의 직선 위에 놓인다." caption="같은 가상 수식을 두 방식으로 읽는다. 오른쪽에서 N이 네 배가 되는 간격마다 감소 항은 절반이 된다. 실제 모델의 실험 결과는 아니다." source="이 노트의 가상 수식으로 직접 계산. E=A=B=1, α=β=0.5, D=1B. N·D는 billion 단위." hero />

## 2. Compute-optimal training

규모와 loss의 관계를 알면 다음 질문으로 이어진다. **같은 계산 예산에서 가장 낮은 loss를 얻으려면 모델 크기와 학습 토큰 수를 어떻게 배분해야 할까?** 모델을 키우는 것과 더 오래 학습하는 것 사이의 선택이다.

### 2-1. 같은 계산량에서의 비교

$C=6\times10^{18}$ FLOPs를 배정한 가상 실험을 생각하자.

| 후보 | 모델 크기 $N$ | 학습 토큰 수 $D$ | 근사 계산량 $6ND$ |
|---|---|---|---|
| A | 0.5B | 2B | $6\times10^{18}$ |
| B | 1B | 1B | $6\times10^{18}$ |
| C | 2B | 0.5B | $6\times10^{18}$ |

계산량은 같지만 어느 후보의 loss가 낮은지는 이 표만으로 알 수 없다. 앞에서 얻은 loss 모형을 이용해 후보를 비교해 보자. 이것이 isoFLOP, 즉 같은 계산량 안에서의 비교다.

<Callout type="example">
교육용으로 $E=1$, $A=B=1$, $\alpha=\beta=0.5$라고 두고 $N,D$를 각각 billion 단위로 넣자. 앞 표의 A는 $1+1/\sqrt{0.5}+1/\sqrt{2}\approx3.121$, B는 3.000, C는 3.121이다. 이 가상 계수에서는 균형 잡힌 B가 낮다. 실제 모델의 loss나 최적 비율을 보고한 수치는 아니다.
</Callout>

<Figure src={isoFlop.src} alt="고정 예산에서 가상 loss가 모델 크기 1B에서 최소가 되는 U자 곡선. 모델이 작으면 모델 항이, 크면 부족한 데이터 항이 loss를 높인다." caption="같은 계산량의 A·B·C를 곡선 위에서 비교한다. 가로축은 로그 척도이고, 오른쪽으로 갈수록 데이터량은 줄어든다." source="이 노트의 가상 수식으로 직접 계산. N과 D는 billion 단위, ND=1." />

<Details summary="Hoffmann 모형에서 고정 예산의 최적 N과 D 유도">

여기서는 $C\approx6ND$를 제약식 $C=6ND$로 놓는다. $N,D$는 실제 개수 단위다. 그림처럼 billion 단위를 쓰면 $C=6\times10^{18}N_{\mathrm B}D_{\mathrm B}$로 바꾸고, loss 식의 계수도 그 단위에 맞춰야 한다. $D=C/(6N)$을 대입하면

$$
\widehat{\mathcal{L}}(N)=E+AN^{-\alpha}+B(6N/C)^\beta
$$

이다. $N$에 대해 미분해 0으로 놓으면

$$
\alpha AN^{-\alpha}=\beta B(6N/C)^\beta
$$

를 얻는다. 따라서 최적점은 두 loss 항이 같은 곳이 아니라 **지수를 곱한 두 항이 같은 곳**이다. $\alpha=\beta$일 때에만 두 항이 같다. 정리하면

$$
N_*=G(C/6)^a,\qquad D_*=G^{-1}(C/6)^b
$$

$$
G=\left(\frac{\alpha A}{\beta B}\right)^{1/(\alpha+\beta)},\qquad
a=\frac{\beta}{\alpha+\beta},\qquad b=\frac{\alpha}{\alpha+\beta}
$$

이다. $a+b=1$이므로 $6N_*D_*=C$를 만족한다. **Loss 감소 지수 $\alpha,\beta$와 예산 배분 지수 $a,b$는 다르다.** 모델 크기의 배분 지수 $a$에는 분자로 $\beta$가 들어간다. 두 감소 지수가 같으면 $a=b=1/2$가 되어, 예산을 네 배 늘릴 때 모델 크기와 토큰 수를 각각 두 배 늘린다. 양수인 두 항이 $\log N$에 대해 엄밀히 볼록하므로 위 정지점은 유일한 최솟값이다.

</Details>

### 2-2. Chinchilla

Chinchilla는 “더 큰 모델”만 비교하는 대신, 같은 계산량에서 모델 크기와 학습 토큰 수를 함께 바꾸었다. 대표 비교에서 Gopher는 280B 파라미터와 300B 토큰, Chinchilla는 70B 파라미터와 1.4T 토큰을 사용했다. Chinchilla는 비슷한 학습 계산 예산에서 여러 평가를 개선했다.[^chinchilla]

이 비교가 주는 질문은 “내 모델이 너무 작은가?”에 더해 <strong>“내 모델 크기에 비해 충분히 학습했는가?”</strong>다. 해당 연구의 결론을 흔히 약 20 tokens/parameter로 요약하지만, 모든 데이터·모델·예산의 보편적인 비율로 적용할 수는 없다.

먼저 두 논문의 예산 배분 결과를 구별하자. 아래는 Hoffmann 논문의 Table 2에 정리된 $N_*\propto C^a$, $D_*\propto C^b$의 지수다.[^chinchilla]

| 연구·추정 방법 | 모델 크기 지수 $a$ | 토큰 수 지수 $b$ |
|---|---|---|
| Kaplan (2020): 계산 효율적인 학습 경계 | 0.73 | 0.27 |
| Hoffmann (2022): 학습 곡선들의 최저 경계 | 0.50 | 0.50 |
| Hoffmann (2022): isoFLOP 곡선의 최저점 | 0.49 | 0.51 |
| Hoffmann (2022): 가산 loss 모형 fitting | 0.46 | 0.54 |

Kaplan의 결과는 학습 도중의 loss와 batch size·학습 step을 함께 분석한 계산 효율적 경계에서 나온다. 원논문의 계산 축은 batch size 효율을 보정한 $C_{\min}$이다. **앞의 데이터셋 크기 식에 단순히 $D=C/(6N)$을 대입해 0.73과 0.27을 얻은 것이 아니다.**[^kaplan]

Hoffmann의 첫 방법은 여러 학습 곡선에서 예산별 최저 loss를 찾고, 둘째는 계산 예산을 고정한 별도 실행들을 비교한다. 셋째만 앞 절의 가산 모형을 직접 맞춘다. 따라서 셋째 방법의 유도로 모든 방법이 정확히 $a=b=0.5$라고 결론 내릴 수 없다. 예산이 10배가 될 때 Kaplan의 보고값은 $N$ 약 5.4배·$D$ 약 1.9배, Hoffmann의 첫 방법은 각각 약 3.2배를 제안한다.

**약 20 tokens/parameter는 앞 수식에서 자동으로 나오는 상수가 아니다.** 실제 Chinchilla의 $1.4\mathrm T/70\mathrm B=20$과, 가산 모형을 fitting해 얻은 최적 비율을 구별한다. 위 해에서는 $D_*/N_*=G^{-2}(C/6)^{b-a}$이므로, $a=b$일 때도 비율은 계수에 의존하고 $a\ne b$이면 예산에 따라서도 달라진다.

[원논문의 Figure 3와 §3.2](https://arxiv.org/html/2203.15556v1#S3.SS2)를 열고 가상 U자 곡선과 비교해 보자. 논문은 이 접근에서 9개 계산 예산을 정하고, 각 예산마다 여러 모델 크기를 학습했다. 그림은 다음 순서로 읽으면 된다.

1. **왼쪽 패널의 곡선 하나를 고른다.** 가로축은 모델 파라미터 수, 세로축은 최종 학습 loss다. 같은 곡선 위에서는 계산량이 고정되어 있으므로 오른쪽으로 갈수록 $N$은 늘고 $D$는 줄어든다. 각 점은 서로 다른 학습 실행의 결과이며, 한 모델이 학습 중 커지는 과정이 아니다.
2. **곡선의 최저점을 찾는다.** 측정한 점들에 곡선을 맞춰 그 예산에서 loss가 가장 낮아지는 모델 크기 $N_*$를 추정한다. 너무 작은 모델과, 주어진 예산으로 충분히 학습하지 못한 큰 모델 사이에 최적점이 나타난다.
3. **다른 예산의 최저점과 비교한다.** 예산이 커질 때 최적 모델 크기와 학습 토큰 수도 어떻게 커지는지 본다. 가운데·오른쪽 패널은 이 최저점들을 모아 $C$에 따른 $N_*,D_*$의 관계를 추정한 것이다.

이 그림의 세로축은 논문에서 사용한 최종 학습 loss다. 앞에서 정의한 평가 loss와 측정 대상이 같다고 넘겨짚지 말고, 실제 모델의 검증 데이터·downstream 결과도 함께 읽는다. 관측 범위 밖으로 예측을 확장하는 **외삽**(extrapolation)은 실제로 측정한 점과 구분한다. Chinchilla는 작은 실험에서 얻은 예측을 더 큰 모델의 학습과 평가로 확인한 사례다.[^chinchilla]

### 2-3. 데이터 반복과 제약

Pre-training에서 데이터 풀의 크기와 학습에서 소비한 토큰 수를 구분했다. 예를 들어 다음 두 실행은 같은 모델을 학습한다면 $6ND$로 계산한 연산량이 같다.

| 구성 | 고유 데이터량 | 평균 반복 횟수 | 소비 토큰 수 $D$ |
|---|---|---|---|
| 새로운 문서를 한 번씩 읽기 | 100B | 1회 | 100B |
| 같은 문서를 반복해서 읽기 | 10B | 10회 | 100B |

그렇다고 두 실행의 최종 loss까지 같다고 예측할 수는 없다. 반복해서 읽으면 이미 본 문맥을 더 학습할 수 있지만, 새로운 문맥의 다양성이 같은 속도로 늘지는 않는다. **연산량을 세는 $D$와 성능을 예측할 때의 데이터 조건을 함께 봐야 한다.**

Muennighoff 등의 *Scaling Data-Constrained Language Models*는 고유 데이터량과 반복 횟수를 구분해 실험했다. 해당 실험에서는 몇 차례 반복이 유용했지만, 반복을 계속 늘릴 때 추가 계산의 이득은 줄었다. 따라서 새로운 데이터로 얻은 scaling 관계에 반복 토큰 수만 대입해 같은 개선을 기대하기는 어렵다.[^data-constrained] [원논문의 Figure 1](https://arxiv.org/html/2305.16264v3#S0.F1)은 반복에 따른 loss 감소와 자원 배분을 함께 보여준다.

한국어·전문 영역처럼 확보할 수 있는 문서가 제한적이라면, 모델 크기와 총 학습 토큰 수에 더해 고유 데이터량·반복 노출·혼합 비율을 기록한다. 데이터 품질이나 혼합을 바꾸었을 때에도 기존 fitting 계수가 그대로 적용되는지는 작은 비교 실험으로 확인해야 한다.

### 2-4. 추론을 포함한 비용

같은 품질을 가진 두 모델이 있다고 가정하자. 하나는 크고 학습을 짧게 했고, 다른 하나는 작지만 더 오래 학습했다. 작은 모델의 학습 비용이 더 들었더라도 앞으로 많은 요청을 처리한다면 배포 과정의 비용을 줄일 수 있다.

$$
C_{\text{lifetime}}=C_{\text{train}}+Q\,C_{\text{request}}
$$

$Q$는 예상 요청 수다. 이 식은 의사결정의 범위를 보여주기 위한 단순 모형이며, 실제 요청 비용은 입력·출력 길이와 batching, 하드웨어 등에 따라 달라진다. LLaMA 논문은 주어진 학습 예산에서만 최적인 모델과 추론을 고려한 모델 설계를 구분한다.[^llama1]

<Callout type="example">
같은 목표 품질에서 모델 A는 학습 비용 100, 요청당 비용 0.01이고, B는 학습 비용 150, 요청당 비용 0.005라고 가정하자. $100+0.01Q=150+0.005Q$이므로 손익분기점(break-even point)은 10,000회다. 금액이나 실제 모델 측정값이 아닌 가상 비용 단위이며, 장기 사용량이 학습 선택을 바꿀 수 있음을 보여준다.
</Callout>

“Compute-optimal보다 오래 학습했다”는 표현은 곧 낭비했다는 뜻이 아니다. 어떤 비용을 목적함수에 넣었는지 먼저 물어야 한다.

### 2-5. 예측의 검증

서베이 *(Mis)Fitting*은 학습 설정, 관측할 실험점, fitting 방식의 차이가 예측과 최적 비율을 바꿀 수 있음을 검토한다.[^survey] 우리 연구의 계획으로 바꾸면 다음 순서가 된다.

1. **비교 조건을 고정한다.** 데이터 분포·tokenizer·평가 텍스트·모델 계열을 명시한다.
2. **여러 크기와 학습 길이를 실험한다.** 한 모델의 중간 checkpoint만으로 모든 규모를 대표하지 않는다.
3. **추정에 쓰지 않은 실험으로 확인한다.** 관측 범위 안에서 곡선이 잘 맞는 것과 외삽이 맞는 것은 다르다.
4. **예측 오차를 예산에 반영한다.** 점추정(point estimate) 하나만 보고 대규모 학습을 결정하기보다 설정 변화에 대한 민감도를 본다.

Pre-training에서 본 learning rate schedule도 이 비교에 영향을 준다. 긴 학습용 schedule의 중간 checkpoint는, 그 시점에 학습을 마치도록 learning rate를 낮춘 별도 실행과 다르다. Chinchilla는 학습 토큰 수에 맞춰 cosine schedule의 길이를 정했다. 같은 $(N,D)$라도 최적화 설정이 다르면 loss와 추정한 최적점이 달라질 수 있다.[^chinchilla]

### 2-6. CS336 보충 자료

Stanford CS336의 2026년 두 Scaling 강의는 논문의 식과 실제 실험 설계를 연결하는 보충 자료다.<Sidenote>Stanford의 [CS336: Language Modeling from Scratch](https://cs336.stanford.edu/)는 데이터 준비부터 언어모델 구현·학습·평가까지 다루는 수업이다. 공식 홈페이지에서 연도별 강의 자료와 과제를 확인할 수 있다.</Sidenote>

- [Lecture 9 영상](https://www.youtube.com/watch?v=Q15rhEWZPQ4): [공식 슬라이드](https://github.com/stanford-cs336/lectures/blob/main/lecture_09.pdf) 43–54쪽에서 두 논문의 공동 scaling 식, Chinchilla의 세 방법, 결과 차이와 추론 비용을 비교한다. 식을 볼 때 괄호의 범위와 파라미터 집계 기준에 주목한다.
- [Lecture 11 영상](https://www.youtube.com/watch?v=vTfEyOyzV9E): [공식 슬라이드](https://github.com/stanford-cs336/lectures/blob/main/lecture_11.pdf) 9–24쪽은 MiniCPM·DeepSeek의 학습률·batch size·학습 길이 설정 사례다. 이 노트에서는 **loss fitting 전에 학습 설정도 맞춰야 한다**는 점을 연결해 읽는다.

Lecture 9는 파라미터 집계와 작은 예산에서의 warmup 문제, Chinchilla의 세 번째 fitting 방법에 대한 후속 재검토도 소개한다. 위 표는 **각 원논문의 보고값**이며, 두 연구의 차이를 수식 하나의 대수 오류나 schedule 하나만의 문제로 해석하지 않는다.

## 3. Emergent abilities

### 3-1. 관측된 능력의 출현

Pre-training에서 평균 NTP loss와 downstream 과제 정확도를 함께 보았다. 여기서도 두 지표를 구분한다. **평균 loss가 조금씩 낮아질 때, 특정 문제를 푸는 성공률도 같은 모양으로 좋아질까?** Wei 등은 작은 모델의 결과만으로 단순 외삽하기 어려운 능력의 출현을 emergent ability라는 관점에서 정리했다.[^emergence]

<Figure hero src={emergenceObserved.src} alt="Wei 등의 Figure 2. 가로축은 학습 FLOPs의 로그 척도, 세로축은 여덟 과제 각각의 평가 점수다. 산술과 MMLU 등에서 작은 모델들의 점수는 낮거나 무작위 기준선 부근에 머물다가 큰 모델에서 높아진다. 색과 기호는 서로 다른 모델 계열을 구분한다." caption="실제 모델 평가에서 관측한 few-shot 과제 성능. 점 하나는 모델 하나이고, 분홍색 점선은 무작위 응답의 기준선이다. (A) 산술과 (G) MMLU 패널을 중심으로 읽는다." source="Wei et al. (2022), Emergent Abilities of Large Language Models, Figure 2 · CC BY 4.0 · PDF에서 그림 영역 발췌, PNG 변환. 그림 내용 수정 없음." />

[그림 원문과 캡션](https://arxiv.org/html/2206.07682v2#S3.F2) · [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)

먼저 **(A) 산술**을 보자. 작은 모델들의 정확도는 0에 가깝고, 더 큰 학습 계산량을 쓴 모델에서 높아진다. **(G) Multi-task NLU**는 MMLU(massive multitask language understanding) 평가다. 작은 모델들의 점수가 무작위 선택 기준선 부근에 모여 있다가 큰 모델에서 상승한다. 두 경우 모두 작은 모델 몇 개만 봤다면 이후의 개선 폭을 예상하기 어려웠을 것이다.

1주차에서 본 **few-shot prompting**으로 평가한 결과이므로, 점수 상승은 평가 예시로 가중치를 갱신한 효과와 구분한다. 또 가로축은 파라미터 수 자체가 아닌 **학습 FLOPs**다. 곡선의 색·기호마다 모델 계열이 다르고 학습 데이터와 설정도 다를 수 있으므로, 하나의 보편적인 임계 모델 크기를 읽어 내는 그림으로 쓰지는 않는다.

하지만 그래프의 급격한 변화만으로 내부 능력이 불연속적으로 생겼다고 결론 내릴 수는 없다. Schaeffer 등은 평가 지표를 바꾸면 일부 급격한 출현이 부드러운 변화로 보일 수 있음을 분석했다.[^mirage]

### 3-2. 평가 지표와 측정 해상도

가상의 5토큰 답변에서 토큰별 정답 확률이 모두 $p$이고, 성공 사건이 독립이라고 가정하자. 전체가 맞아야 1점인 완전 일치(exact match) 점수의 기댓값(expected value)은 $p^5$다.

| 토큰별 정답 확률 $p$ | 전체 5토큰이 맞을 확률 $p^5$ |
|---|---|
| 0.4 | 1.02% |
| 0.6 | 7.78% |
| 0.8 | 32.77% |
| 0.9 | 59.05% |

<Figure src={emergenceMetric.src} alt="동일한 예측을 평가할 때 토큰별 성공률 p는 직선이고 전체 정답률 p의 5제곱은 후반에서 가파르게 증가하는 연속 곡선이다." caption="두 곡선 모두 연속이다. 이 그림은 불연속적인 능력 출현의 증거가 아니라, 채점 방식이 곡선 모양을 바꾸는 예제다. 가로축은 모델 크기가 아닌 토큰별 성공 확률이다." source="본문의 독립적인 5토큰 성공 가정으로 직접 계산한 교육용 예제." />

토큰별 성능은 조금씩 좋아져도 전체 정답률은 뒤쪽에서 더 가파르게 보인다. 평가 문항이 적거나 모델 크기를 몇 개만 측정하면 낮은 구간은 모두 0점으로 관측될 수도 있다. 실제 언어의 토큰 오류는 독립이 아니므로, 이 표는 메커니즘의 가능성을 보여주는 단순 예제다.

여기서 가로축은 모델 크기가 아니라 $p$다. 모델 크기를 늘렸을 때 $p$가 어떻게 변하는지는 별도로 측정해야 하므로, 이 예제만으로 특정 규모에서 능력이 생긴다고 예측할 수는 없다.

측정 해상도의 영향도 계산해 보자. $p=0.4$일 때 답 전체의 성공률은 약 0.01024다. 독립적인 평가 문항 20개만 쓰면 성공 횟수의 기댓값은 약 0.205개이고, 전부 실패할 확률은 $(1-0.01024)^{20}\approx81.4\%$다. 실제 성공률이 조금씩 개선되어도 작은 평가에서는 계속 0점으로 보일 수 있는 이유다. 이 역시 독립성 등을 가정한 교육용 계산이다.

이 논쟁에서 구분할 것은 **능력의 개선**, **그 개선이 급격한가**, **작은 실험으로 예측할 수 있는가**다. 지표 효과를 지적하는 것이 모델의 유용한 능력 향상을 부정하는 것은 아니다. 반대로 성공률 곡선 하나만으로 새로운 메커니즘의 출현을 입증한 것도 아니다.

실제 지표 비교는 [Schaeffer 등의 Figure 3](https://arxiv.org/html/2304.15004v2#S3.F3)에서 볼 수 있다. 위쪽은 산술 응답 전체의 정답 여부를 평가하고, 아래쪽은 정답에 도달하기 위해 토큰을 얼마나 고쳐야 하는지 보는 token edit distance로 평가한다. 같은 모델 계열의 출력도 채점 방식에 따라 개선 곡선이 달라진다.[^mirage] 앞의 $p^5$는 이 가능성을 이해하는 작은 예제이며, 모든 과제의 emergence를 설명했다는 뜻은 아니다.

계산 예산과 목표 품질을 바탕으로 $N,D$를 정했다면, 다음 문제는 그 학습을 실제 GPU에서 수행하는 것이다. Training engineering에서는 가중치·gradient·optimizer 상태를 저장하고 계산과 통신을 분산하는 방법을 살펴본다. 여기서 센 연산량이 학습 시간으로 어떻게 이어지는지도 그 실행 방식에 달려 있다.

[^survey]: Li, M., Kudugunta, S., & Zettlemoyer, L. (2025). *(Mis)Fitting: A Survey of Scaling Laws*. [arXiv:2502.18969v1](https://arxiv.org/abs/2502.18969v1).
[^kaplan]: Kaplan, J., et al. (2020). *Scaling Laws for Neural Language Models*. [arXiv:2001.08361](https://arxiv.org/abs/2001.08361).
[^chinchilla]: Hoffmann, J., et al. (2022). *Training Compute-Optimal Large Language Models*. NeurIPS 2022. [arXiv:2203.15556v1](https://arxiv.org/abs/2203.15556v1).
[^llama1]: Touvron, H., et al. (2023). *LLaMA: Open and Efficient Foundation Language Models*. [arXiv:2302.13971v1](https://arxiv.org/abs/2302.13971v1).
[^emergence]: Wei, J., et al. (2022). *Emergent Abilities of Large Language Models*. Transactions on Machine Learning Research. [arXiv:2206.07682v2](https://arxiv.org/abs/2206.07682v2).
[^mirage]: Schaeffer, R., Miranda, B., & Koyejo, S. (2023). *Are Emergent Abilities of Large Language Models a Mirage?* NeurIPS 2023. [arXiv:2304.15004v2](https://arxiv.org/abs/2304.15004v2).
[^data-constrained]: Muennighoff, N., et al. (2023). *Scaling Data-Constrained Language Models*. [arXiv:2305.16264v3](https://arxiv.org/abs/2305.16264v3).
