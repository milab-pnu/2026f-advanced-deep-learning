---
title: "Transformers — Attention Is All You Need"
week: 1
---

이 노트는 Vaswani 등의 2017년 논문 <em>Attention Is All You Need</em><Cite n={1} /> 를
1차 자료로 삼아 Transformer 아키텍처를 수식 수준에서 다시 조립한다. 설명 방식과
그림은 이 주제를 가장 잘 풀어낸 해설들 — Alammar 의 *The Illustrated
Transformer*<Cite n={4} />, Harvard NLP 의 *The Annotated Transformer*<Cite n={5} />,
Weng 의 *Attention? Attention!*<Cite n={6} />, Bloem 의 *Transformers from
Scratch*<Cite n={7} /> — 에서 가져왔다. 각 구성요소를 **무엇을 계산하는가 · 왜
그렇게 하는가 · 작은 예시** 순서로 본다. 코드는 다음 실습 노트에서 직접 구현한다.

<Figure hero
  src="https://upload.wikimedia.org/wikipedia/commons/3/34/Transformer%2C_full_architecture.png"
  alt="Transformer 전체 아키텍처 — 왼쪽 encoder 스택, 오른쪽 decoder 스택"
  caption="Transformer 전체 구조. 왼쪽이 encoder, 오른쪽이 decoder. 각 스택은 동일한 블록 N개를 쌓은 것이다."
  source="Wikimedia Commons “Transformer, full architecture” (Vaswani et al., 2017, Figure 1 기반 재작도)" />

## 1. 순차 모델의 벽

2017년 이전의 기계번역(neural machine translation)은 대부분 **RNN 기반 encoder–decoder**
구조였다. encoder 가 source 문장을 왼쪽에서 오른쪽으로 한 토큰씩 읽어 hidden state
$h_t = f(h_{t-1}, x_t)$ 를 갱신하고, decoder 가 그 정보를 받아 target 문장을 한 토큰씩
생성한다. 여기에 attention 을 얹어 decoder 가 매 스텝 source 의 특정 위치를 "돌아보게"
한 것이 Bahdanau 등의 기여였다.<Cite n={2} />

이 방식에는 두 가지 구조적 한계가 있다.

<Callout type="warning">
**순차 계산.** $h_t$ 를 구하려면 $h_{t-1}$ 이 있어야 한다. 길이 $n$ 문장의 forward
pass 는 $O(n)$ 번의 순차 스텝을 강제하고, 이 사슬은 병렬화할 수 없다. GPU 는 놀고
시퀀스가 길수록 학습이 느려진다.
</Callout>

<Callout type="warning">
**장거리 의존성.** 문장 첫 단어의 정보가 마지막 단어에 닿으려면 $n$개의 RNN 스텝을
통과해야 한다. 경로가 길수록 gradient 가 소실·폭발하기 쉽고, 멀리 떨어진 단어 사이의
관계를 학습하기 어렵다.
</Callout>

Transformer 의 출발점은 이것이다: **recurrence 를 완전히 버리고, 시퀀스 안의 모든 위치
쌍을 attention 한 번으로 직접 연결하면 어떨까?** 그러면 위치 $i$ 와 $j$ 사이의 경로
길이는 항상 1이고, 한 레이어의 계산은 전부 병렬로 돌릴 수 있다.

## 2. 큰 그림

Transformer 도 encoder–decoder 다. 다만 두 스택 모두 recurrence 나 convolution 없이
**attention 과 position-wise feed-forward 두 종류의 연산만** 쌓는다.

- **Encoder**: 동일한 구조의 레이어 $N=6$ 개.<Sidenote>"동일한 레이어"는 구조가 같다는 뜻이지 가중치를 공유한다는 뜻이 아니다. 6개 레이어는 각자 별도의 파라미터를 가진다.</Sidenote> 각 레이어는 (1) multi-head **self-attention** + (2) position-wise **feed-forward network** 두 sub-layer 로 구성. source 시퀀스를 받아 같은 길이의 표현 시퀀스 $z_1, \dots, z_n$ 을 출력한다.
- **Decoder**: 역시 $N=6$ 개 레이어. encoder 레이어에 sub-layer 하나가 추가된다 —
  encoder 출력을 향한 **cross-attention**. 또한 decoder 의 self-attention 은 미래
  토큰을 못 보도록 **masking** 된다(아래 6절).
- 모든 sub-layer 출력에는 **residual connection** 과 **layer normalization** 이 붙는다.
- 임베딩 차원은 전 구간에서 $d_{\text{model}} = 512$ 로 고정된다 (residual 을 더하려면
  차원이 같아야 한다).

Alammar 는 이 구조를 "encoder 를 여섯 개 쌓고, 같은 수의 decoder 를 쌓은 것"으로
그린다 — 스택 안의 블록은 전부 똑같이 생겼고, 우리가 이해해야 할 것은 블록 하나뿐이다.<Cite n={4} />
이제 그 블록의 핵심 부품인 attention 부터 본다.

## 3. Attention 이라는 연산

### 3.1 Soft dictionary lookup

attention 은 **부드러운 딕셔너리 조회(soft dictionary lookup)** 로 이해하는 것이 가장 빠르다.

- 우리가 찾고자 하는 것을 나타내는 벡터가 **query** $q$.
- 각 저장 항목에는 색인 역할을 하는 **key** $k_j$ 와, 실제 내용인 **value** $v_j$ 가 있다.
- 조회는 $q$ 와 각 $k_j$ 의 유사도를 재서, 그 유사도를 가중치로 $v_j$ 들을 섞어 돌려준다.

하드 딕셔너리라면 "정확히 일치하는 key 하나의 value 만 반환"하지만, attention 은 유사도를
softmax 로 정규화해 **모든 value 의 가중 평균**을 반환한다. 그래서 미분 가능하고 학습된다.
Weng 은 attention 을 아예 "중요도 가중치 벡터"라고 부른다 — 각 출력 위치가 입력의 어느
부분에 얼마나 기대야 하는지를 나타내는 분포다.<Cite n={6} />

유사도를 어디서 얻는가? Bloem 의 직관이 명쾌하다. 영화 추천에서 사용자 특성벡터와 영화
특성벡터의 **내적**이 클수록 그 사용자가 그 영화를 좋아할 가능성이 크다. 마찬가지로 두
토큰 임베딩의 내적은 "이 학습 태스크의 관점에서 두 토큰이 얼마나 관련되어 있는가"를
나타내도록 학습된다.<Cite n={7} />

### 3.2 예시: 대명사는 무엇을 가리키는가

Alammar 의 예문을 보자: *"The animal didn't cross the street because it was too tired."*
사람은 *it* 이 *animal* 을 가리킨다는 것을 안다. self-attention 이 하는 일이 정확히
이것이다 — *it* 의 표현을 갱신할 때, *it* 의 query 가 *animal* 의 key 와 높은 내적을
갖도록 학습되면, *it* 의 새 표현은 *animal* 의 value 를 많이 담게 된다.

<Figure
  src="https://jalammar.github.io/images/t/transformer_self-attention_visualization.png"
  alt="self-attention 시각화 — 토큰 'it' 이 'The animal' 에 높은 attention weight 를 주는 히트맵"
  caption="학습된 Transformer encoder 에서 'it' 위치의 attention 분포. 색이 진할수록 큰 가중치이며, 'The animal' 쪽으로 쏠려 있다."
  source="Jay Alammar, “The Illustrated Transformer” (Tensor2Tensor 시각화)" />

<Callout type="intuition">
attention 은 "어디를 봐야 하는가"를 규칙으로 넣어주는 게 아니라 **데이터로부터 배운다.**
어떤 head 는 대명사 해소를, 어떤 head 는 인접 단어를, 어떤 head 는 구문 구조를 담당하게
되는 식이다(4절).
</Callout>

### 3.3 Scaled Dot-Product Attention

유사도로는 **내적(dot product)** 을 쓴다. query 하나 $q \in \mathbb{R}^{d_k}$ 와
key 행렬 $K \in \mathbb{R}^{m \times d_k}$, value 행렬 $V \in \mathbb{R}^{m \times d_v}$
에 대해:

$$
\mathrm{Attention}(q, K, V) = \sum_{j=1}^{m} \underbrace{\frac{\exp(q \cdot k_j / \sqrt{d_k})}{\sum_{l} \exp(q \cdot k_l / \sqrt{d_k})}}_{\text{attention weight } \alpha_j} \, v_j
$$

실제로는 query 도 한꺼번에 처리한다. $n$개의 query 를 행으로 쌓은
$Q \in \mathbb{R}^{n \times d_k}$ 에 대해 전체 연산은 행렬 두 번의 곱과 한 번의
softmax 로 끝난다:

$$
\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\!\left( \frac{Q K^{\top}}{\sqrt{d_k}} \right) V
$$

여기서 softmax 는 행 단위로 적용된다. 결과는 $n \times d_v$ 행렬이고, $i$번째 행은
$i$번째 query 에 대한 value 들의 가중 평균이다. Alammar 는 이 한 줄이 사실은 여섯
단계(Q·K·V 만들기 → 내적으로 점수 → $\sqrt{d_k}$ 로 나누기 → softmax → value 에
가중 → 합산)를 행렬로 압축한 것이라고 풀어 설명한다.<Cite n={4} />

<figure class="lecture-figure">
  <svg viewBox="0 0 640 220" role="img" aria-label="Scaled dot-product attention 의 계산 흐름: Q와 K의 곱, 스케일링, softmax, 그리고 V와의 곱">
    <rect x="14" y="24" width="60" height="36" rx="7" fill="#e2e8f0" stroke="#0f172a" stroke-width="1.5" />
    <text x="44" y="47" text-anchor="middle" font-size="16" fill="#0f172a">V</text>
    <rect x="14" y="92" width="60" height="36" rx="7" fill="#e2e8f0" stroke="#0f172a" stroke-width="1.5" />
    <text x="44" y="115" text-anchor="middle" font-size="16" fill="#0f172a">Q</text>
    <rect x="14" y="150" width="60" height="36" rx="7" fill="#e2e8f0" stroke="#0f172a" stroke-width="1.5" />
    <text x="44" y="173" text-anchor="middle" font-size="16" fill="#0f172a">K</text>

    <rect x="110" y="95" width="108" height="50" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="164" y="116" text-anchor="middle" font-size="16" fill="#0f172a">MatMul</text>
    <text x="164" y="134" text-anchor="middle" font-size="13" fill="#64748b">Q Kᵀ</text>

    <rect x="252" y="95" width="108" height="50" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="306" y="116" text-anchor="middle" font-size="16" fill="#0f172a">scale</text>
    <text x="306" y="134" text-anchor="middle" font-size="13" fill="#64748b">÷ √dₖ</text>

    <rect x="394" y="95" width="108" height="50" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="448" y="125" text-anchor="middle" font-size="16" fill="#0f172a">softmax</text>

    <rect x="520" y="64" width="108" height="50" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="574" y="85" text-anchor="middle" font-size="16" fill="#0f172a">MatMul</text>
    <text x="574" y="103" text-anchor="middle" font-size="13" fill="#64748b">× V</text>

    <rect x="520" y="16" width="108" height="34" rx="7" fill="#0f172a" />
    <text x="574" y="38" text-anchor="middle" font-size="15" fill="#ffffff">output</text>

    <line x1="74" y1="110" x2="110" y2="112" stroke="#0f172a" stroke-width="1.6" />
    <line x1="74" y1="168" x2="110" y2="128" stroke="#0f172a" stroke-width="1.6" />
    <line x1="218" y1="120" x2="252" y2="120" stroke="#0f172a" stroke-width="1.6" />
    <line x1="360" y1="120" x2="394" y2="120" stroke="#0f172a" stroke-width="1.6" />
    <line x1="502" y1="120" x2="574" y2="114" stroke="#0f172a" stroke-width="1.6" />
    <line x1="74" y1="42" x2="574" y2="64" stroke="#94a3b8" stroke-width="1.6" stroke-dasharray="5 4" />
    <line x1="574" y1="64" x2="574" y2="50" stroke="#0f172a" stroke-width="1.6" />
  </svg>
  <figcaption>Scaled dot-product attention 의 데이터 흐름. 점선은 value $V$ 가 마지막 곱까지 그대로 전달됨을 나타낸다.</figcaption>
</figure>

### 3.4 왜 √dₖ 로 나누는가

이 스케일링은 논문의 작지만 중요한 디테일이다. 논문의 설명은 이렇다: "$d_k$ 가 크면
내적이 크게 자라 softmax 를 gradient 가 극도로 작은 영역으로 밀어넣는다."<Cite n={1} />
Annotated Transformer 는 그 이유를 한 문장으로 정리한다 — $q$ 와 $k$ 의 각 성분이
평균 0, 분산 1인 독립 확률변수면, 내적 $q \cdot k = \sum_i q_i k_i$ 는 평균 0,
**분산 $d_k$** 다.<Cite n={5} />

<Callout type="example">
$d_k = 64$ 이면 내적의 표준편차는 $\sqrt{64} = 8$. 즉 score 들이 대략 $\pm 8$ 범위에
흩어진다. softmax 는 입력 간 차이가 이 정도만 되어도 거의 one-hot 을 출력한다.
$1/\sqrt{d_k}$ 로 나누면 분산이 다시 1로 돌아와 score 가 $\pm 1$ 근처에 모인다.
</Callout>

내적 값이 크면 softmax 출력이 거의 one-hot 에 가까워지고, 이 영역에서 softmax 의
Jacobian 은 거의 0이라 **gradient 가 사라진다**. 스케일을 $1/\sqrt{d_k}$ 로 잡으면
내적의 분산이 다시 1 근처로 돌아와 softmax 가 민감한 영역에서 동작한다. Bloem 은 이를
"내적의 평균 크기가 임베딩 차원과 함께 자라기 때문"이라고 같은 말로 요약한다.<Cite n={7} />

<Details summary="softmax 포화와 gradient 소실 — 조금 더 정확히">
$p = \mathrm{softmax}(s)$ 일 때 $\dfrac{\partial p_i}{\partial s_j} = p_i(\delta_{ij} - p_j)$
이다. 어떤 $p_k \to 1$ 이면 모든 $j$ 에 대해 $\dfrac{\partial p_k}{\partial s_j} \to 0$
이고, 다른 성분들의 미분도 전부 0으로 수렴한다. 따라서 attention score 가
과도하게 크면 상류로 전달되는 gradient 가 거의 없어져, attention 패턴이 초기값 근처에
얼어붙는다. $\sqrt{d_k}$ 스케일은 학습 초기에 score 분포를 표준정규 근처로 유지해
이 포화를 막는 역할을 한다. 실전에서는 이 스케일을 빼면 큰 $d_k$ 에서 학습이 눈에
띄게 불안정해진다.
</Details>

## 4. Multi-Head Attention

$d_{\text{model}}$ 차원 공간에서 attention 을 한 번만 수행하면, 모델은 "한 종류의
관계"만 포착하도록 강제된다. 대신 표현을 여러 개의 저차원 부분공간으로 나눠, 각
부분공간에서 **독립적으로 attention 을 병렬 수행**한 뒤 결과를 합친다. Alammar 는 이
이점을 두 가지로 정리한다 — (1) 모델이 여러 위치에 동시에 집중할 수 있고, (2) attention
레이어가 여러 개의 "표현 부분공간(representation subspaces)"을 갖게 된다.<Cite n={4} />

$h = 8$ 개의 head 각각에 대해 학습된 projection 행렬
$W_i^Q, W_i^K \in \mathbb{R}^{d_{\text{model}} \times d_k}$,
$W_i^V \in \mathbb{R}^{d_{\text{model}} \times d_v}$ 로 입력을 투영하고:

$$
\mathrm{head}_i = \mathrm{Attention}(Q W_i^Q,\; K W_i^K,\; V W_i^V)
$$

$$
\mathrm{MultiHead}(Q,K,V) = \mathrm{Concat}(\mathrm{head}_1, \dots, \mathrm{head}_h)\, W^O
$$

논문은 $d_k = d_v = d_{\text{model}} / h = 64$ 로 잡는다. 이렇게 하면 head 를 나눠도
전체 연산량이 single-head full-dimension attention 과 비슷하다.

<figure class="lecture-figure">
  <svg viewBox="0 0 640 220" role="img" aria-label="Multi-head attention: 입력을 h개의 head로 투영해 병렬로 attention 을 수행한 뒤 concat, 마지막에 선형 투영">
    <rect x="14" y="90" width="76" height="40" rx="7" fill="#e2e8f0" stroke="#0f172a" stroke-width="1.5" />
    <text x="52" y="114" text-anchor="middle" font-size="15" fill="#0f172a">Q, K, V</text>

    <rect x="170" y="26" width="156" height="32" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="248" y="46" text-anchor="middle" font-size="13" fill="#0f172a">Attention · head 1</text>
    <rect x="170" y="92" width="156" height="32" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="248" y="112" text-anchor="middle" font-size="13" fill="#0f172a">Attention · head 2</text>
    <text x="248" y="146" text-anchor="middle" font-size="18" fill="#64748b">⋮</text>
    <rect x="170" y="156" width="156" height="32" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="248" y="176" text-anchor="middle" font-size="13" fill="#0f172a">Attention · head h</text>

    <rect x="396" y="88" width="88" height="52" rx="7" fill="#f8fafc" stroke="#0f172a" stroke-width="1.5" />
    <text x="440" y="119" text-anchor="middle" font-size="15" fill="#0f172a">Concat</text>

    <rect x="520" y="88" width="88" height="52" rx="7" fill="#0f172a" />
    <text x="564" y="110" text-anchor="middle" font-size="14" fill="#ffffff">Linear</text>
    <text x="564" y="127" text-anchor="middle" font-size="12" fill="#cbd5e1">Wᴼ</text>

    <line x1="90" y1="108" x2="170" y2="42" stroke="#94a3b8" stroke-width="1.6" />
    <line x1="90" y1="110" x2="170" y2="108" stroke="#94a3b8" stroke-width="1.6" />
    <line x1="90" y1="112" x2="170" y2="172" stroke="#94a3b8" stroke-width="1.6" />
    <line x1="326" y1="42" x2="396" y2="100" stroke="#0f172a" stroke-width="1.6" />
    <line x1="326" y1="108" x2="396" y2="114" stroke="#0f172a" stroke-width="1.6" />
    <line x1="326" y1="172" x2="396" y2="128" stroke="#0f172a" stroke-width="1.6" />
    <line x1="484" y1="114" x2="520" y2="114" stroke="#0f172a" stroke-width="1.6" />
  </svg>
  <figcaption>Multi-head attention. 입력을 $h$개의 저차원 부분공간으로 투영해 병렬로 attention 을 수행한다.</figcaption>
</figure>

<Callout type="intuition">
서로 다른 head 는 서로 다른 관계를 배우는 경향이 있다 — 어떤 head 는 바로 옆 단어에,
어떤 head 는 구문상 지배소(head word)에, 어떤 head 는 문장 전체에 고르게 주의를
분산한다. Multi-head 는 "여러 관점에서 동시에 문장을 읽는" 장치라고 볼 수 있다.
</Callout>

## 5. Positional Encoding

attention 은 집합 연산이다 — 입력 순서를 바꾸면 출력도 그대로 순서만 바뀔 뿐,
"몇 번째 위치인가"라는 정보가 없다. Alammar 의 표현으로는 "지금까지 설명한 모델에는
단어의 순서를 다룰 방법이 빠져 있다."<Cite n={4} /> recurrence 를 버렸으니 위치 정보를
명시적으로 주입해야 한다.

논문은 학습 파라미터 없이 고정된 **정현파(sinusoidal) positional encoding** 을 쓴다.
위치 $pos$, 차원 인덱스 $i$ 에 대해:

$$
PE_{(pos,\, 2i)} = \sin\!\left( \frac{pos}{10000^{2i/d_{\text{model}}}} \right), \qquad
PE_{(pos,\, 2i+1)} = \cos\!\left( \frac{pos}{10000^{2i/d_{\text{model}}}} \right)
$$

이 벡터를 토큰 임베딩에 **더한다**.<Sidenote>논문은 학습되는 positional embedding 도 실험했고 성능은 거의 같았다. 정현파를 택한 이유는 학습 때 본 것보다 긴 시퀀스로도 외삽(extrapolate)될 가능성 때문이다.</Sidenote>

<Figure
  src="https://jalammar.github.io/images/t/transformer_positional_encoding_large_example.png"
  alt="positional encoding 히트맵 — 20개 위치 × 512 차원, 왼쪽 절반은 sine, 오른쪽 절반은 cosine 패턴"
  caption="위치 20개(행) × 차원 512개(열)의 positional encoding. 낮은 차원은 위치에 따라 빠르게 진동하고, 높은 차원은 천천히 진동한다."
  source="Jay Alammar, “The Illustrated Transformer”" />

<Callout type="intuition">
차원마다 파장이 기하급수적으로 달라지는 정현파를 겹쳐놓은 것이다. 낮은 차원은
빠르게 진동(가까운 위치 구분), 높은 차원은 천천히 진동(멀리 떨어진 위치 구분).
게다가 삼각함수 덧셈정리에 의해 $PE_{pos+k}$ 는 $PE_{pos}$ 의 **선형변환**으로
표현된다 — 모델이 "상대 위치 $k$ 만큼 이동"을 선형 연산으로 배우기 쉽다.
</Callout>

## 6. Transformer 안의 세 가지 Attention

같은 multi-head attention 블록이 위치에 따라 세 가지 역할을 한다.

1. **Encoder self-attention.** $Q, K, V$ 가 모두 직전 encoder 레이어의 출력. 각 위치가
   source 문장의 모든 위치를 본다. 마스킹 없음.
2. **Decoder masked self-attention.** $Q, K, V$ 가 모두 직전 decoder 레이어의 출력.
   단, 위치 $i$ 는 $j \le i$ 인 위치만 볼 수 있다. 미래 토큰을 보면 학습 시 정답을
   컨닝하게 되므로, softmax 이전 score 행렬에서 $j > i$ 인 원소를 $-\infty$ 로
   채운다(look-ahead mask).
3. **Encoder–decoder (cross) attention.** $Q$ 는 직전 decoder 레이어에서, $K, V$ 는
   **encoder 최종 출력**에서 온다. decoder 의 각 위치가 source 문장 전체를 참조한다 —
   기존 seq2seq attention 이 하던 일이 바로 이것이다.

<Callout type="note">
inference 때 decoder 는 여전히 토큰을 하나씩 autoregressive 하게 생성한다. Transformer
가 없앤 것은 **학습 시**의 순차 사슬이다 — teacher forcing 으로 정답 시퀀스를 통째로
넣고 masked self-attention 으로 한 번에 병렬 학습한다.
</Callout>

## 7. Position-wise Feed-Forward Network

각 attention sub-layer 뒤에는 **위치마다 독립적으로** 적용되는 2층 MLP 가 붙는다.

$$
\mathrm{FFN}(x) = \max(0,\; x W_1 + b_1)\, W_2 + b_2
$$

$W_1 \in \mathbb{R}^{d_{\text{model}} \times d_{ff}}$,
$W_2 \in \mathbb{R}^{d_{ff} \times d_{\text{model}}}$ 이고 논문은 $d_{ff} = 2048$ 을 쓴다
(즉 안쪽에서 4배로 넓혔다가 다시 좁힌다). 같은 레이어 안에서는 모든 위치가 같은
$W_1, W_2$ 를 공유하지만, 레이어마다는 다르다.

<Callout type="note">
attention 이 "위치들 사이에서 정보를 섞는" 연산이라면, FFN 은 "각 위치의 표현을
비선형으로 가공하는" 연산이다. 두 연산이 번갈아 쌓이면서 mixing 과 transformation 을
반복한다. FFN 은 $1 \times 1$ convolution 두 개로 봐도 동일하다.
</Callout>

## 8. Residual Connection 과 Layer Normalization

모든 sub-layer 는 다음 형태로 감싸진다:

$$
\mathrm{output} = \mathrm{LayerNorm}\big( x + \mathrm{Sublayer}(x) \big)
$$

- **Residual connection** $x + \mathrm{Sublayer}(x)$ 는 6개 레이어를 관통하는 짧은
  gradient 경로를 만들어 깊은 스택의 학습을 가능하게 한다. residual 을 더하려면
  입력과 출력 차원이 같아야 하므로 전 구간 $d_{\text{model}} = 512$ 다.
- **Layer normalization**<Cite n={3} /> 은 각 위치의 특성벡터를 그 벡터 안에서
  평균 0, 분산 1로 정규화한다(batch 축이 아니라 feature 축). 배치 크기·시퀀스 길이에
  무관하게 안정적이라 시퀀스 모델에 잘 맞는다.

<Figure
  src="https://jalammar.github.io/images/t/transformer_resideual_layer_norm_2.png"
  alt="encoder 레이어에서 각 sub-layer 를 residual connection 과 layer normalization 이 감싸는 구조"
  caption="한 encoder 레이어. self-attention 과 feed-forward 각각을 'Add & Normalize' 가 감싼다."
  source="Jay Alammar, “The Illustrated Transformer”" />

## 9. 전체 조립

한 **encoder 레이어**:

```
x1 = LayerNorm(x  + MultiHeadSelfAttention(x))
x2 = LayerNorm(x1 + FFN(x1))
```

한 **decoder 레이어**:

```
y1 = LayerNorm(y  + MaskedMultiHeadSelfAttention(y))
y2 = LayerNorm(y1 + CrossAttention(query=y1, key=value=encoderOutput))
y3 = LayerNorm(y2 + FFN(y2))
```

전체 forward:

1. source 토큰 → 임베딩 + positional encoding → encoder 6레이어 → $z$ (source 표현)
2. target 토큰(shift right) → 임베딩 + positional encoding → decoder 6레이어(매
   레이어에서 $z$ 를 cross-attention 으로 참조)
3. decoder 출력 → 임베딩 행렬과 묶인(tied) linear → softmax → 다음 토큰 분포<Sidenote>입력·출력 임베딩 행렬과 최종 pre-softmax linear 는 같은 가중치를 공유한다(weight tying). 파라미터를 아끼고 대체로 성능도 좋아진다.</Sidenote>

## 10. 왜 되는가 — 복잡도와 경로 길이

논문의 핵심 논거는 세 축의 비교표다 ($n$ = 시퀀스 길이, $d$ = 표현 차원,
$k$ = convolution 커널 크기).

| 레이어 종류 | 레이어당 복잡도 | 순차 연산 | 최대 경로 길이 |
|---|---|---|---|
| Self-Attention | $O(n^2 \cdot d)$ | $O(1)$ | $O(1)$ |
| Recurrent | $O(n \cdot d^2)$ | $O(n)$ | $O(n)$ |
| Convolutional | $O(k \cdot n \cdot d^2)$ | $O(1)$ | $O(\log_k n)$ |
| Self-Attention (윈도우 $r$) | $O(r \cdot n \cdot d)$ | $O(1)$ | $O(n/r)$ |

읽는 법:

- **순차 연산 $O(1)$**: self-attention 한 레이어는 시퀀스 길이와 무관하게 상수 번의
  행렬곱으로 끝난다 → 완전 병렬.
- **최대 경로 길이 $O(1)$**: 어떤 두 위치든 한 번의 attention 으로 직접 연결 →
  장거리 의존성 학습에 유리.
- **비용**: 대신 레이어당 계산이 $n^2$ 에 비례한다. $n < d$ 인 전형적 문장 번역
  ($n \approx 40$, $d = 512$)에서는 recurrent 보다 오히려 싸지만, 문서 단위로 $n$ 이
  커지면 이 $n^2$ 항이 Transformer 후속 연구의 최대 관심사가 된다.

<Callout type="intuition">
Transformer 의 거래는 이렇다: **순차 깊이(느림, 학습 난이도)를 시퀀스 길이 제곱의
계산량(병렬화 가능, 메모리)으로 바꿨다.** GPU 시대에는 남는 장사였다.
</Callout>

## 11. 학습 레시피

아키텍처만큼이나 재현에 중요한 디테일들. Annotated Transformer 는 이 설정들을 그대로
코드로 옮겨 놓았다.<Cite n={5} />

- **Optimizer**: Adam, $\beta_1 = 0.9$, $\beta_2 = 0.98$, $\epsilon = 10^{-9}$.
- **Learning rate schedule**: warmup 후 역제곱근 감쇠.

  $$
  lr = d_{\text{model}}^{-0.5} \cdot \min\!\left( step^{-0.5},\; step \cdot warmup^{-1.5} \right)
  $$

  $warmup = 4000$ steps. 초반에는 선형 증가, 이후 $step^{-0.5}$ 로 감소.
- **Regularization**: 모든 sub-layer 출력과 임베딩 합에 dropout $P_{drop} = 0.1$;
  label smoothing $\epsilon_{ls} = 0.1$ (perplexity 는 나빠지지만 BLEU·정확도는 개선).
- **Hardware / 시간**: 8× NVIDIA P100. base 모델 100k steps ≈ 12시간, big 모델
  300k steps ≈ 3.5일.
- **데이터**: WMT 2014 English–German (약 4.5M 문장쌍, byte-pair encoding 37k 공유
  어휘), English–French (약 36M 문장쌍, word-piece 32k).

<Callout type="warning">
warmup 없는 Adam 은 Transformer 학습 초기에 자주 발산한다. LR schedule 은 선택이
아니라 사실상 필수 부품이다. 이후 연구들이 pre-LN 구조, RAdam 등으로 이 민감도를
완화한다.
</Callout>

## 12. 결과와 그 이후

- **WMT 2014 EN→DE**: big 모델 28.4 BLEU — 당시 앙상블 포함 최고 기록을 2 BLEU 이상
  경신.
- **WMT 2014 EN→FR**: big 모델 41.8 BLEU — 기존 단일 모델 SOTA 를, 8 GPU 3.5일이라는
  기존 대비 일부 비용만으로 갱신.
- **English constituency parsing** 에서도 잘 동작 — 번역 전용 트릭이 아니라 범용
  시퀀스 모델임을 시사.

Transformer 는 곧 번역을 넘어 확장된다. encoder 만 떼어내 양방향 마스킹 언어모델로
사전학습한 것이 **BERT**<Cite n={8} />, decoder 만 떼어내 autoregressive 언어모델로
키운 것이 **GPT** 계열이다. 오늘날의 대규모 언어모델은 사실상 전부 이 노트에서 조립한
블록의 스케일업이다.

<Video src="https://www.youtube.com/watch?v=eMlx5fFNoYc"
  caption="Attention in transformers, visually explained — 3Blue1Brown, Chapter 6 (attention 의 시각적 직관)" />

## 13. 정리

- Transformer 는 recurrence·convolution 없이 **attention + position-wise FFN** 만으로
  seq2seq 를 수행한다.
- **Scaled dot-product attention** 은 query–key 내적을 $\sqrt{d_k}$ 로 스케일한 뒤
  softmax 가중 평균으로 value 를 섞는다.
- **Multi-head** 는 여러 부분공간에서 attention 을 병렬 수행해 다양한 관계를 포착한다.
- **Positional encoding** 이 순서 정보를, **residual + LayerNorm** 이 깊은 스택의
  학습 가능성을, **warmup LR schedule** 이 초기 안정성을 담당한다.
- 대가로 레이어당 계산이 $O(n^2 d)$ 지만, 순차 깊이 $O(1)$·경로 길이 $O(1)$ 을 얻어
  병렬 학습과 장거리 의존성에서 크게 앞선다.

다음 노트에서 이 구조를 PyTorch 로 처음부터 구현하고, 작은 번역 태스크에서 학습시켜
본다. 그 전에 위 **더 읽을거리** 중 Annotated Transformer 를 한 번 훑어보면, 각 수식이
어떤 코드 한 줄로 옮겨지는지 미리 감을 잡을 수 있다.

<References items={[
  { id: 1, text: "Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., Kaiser, Ł., & Polosukhin, I. (2017). Attention Is All You Need. In Advances in Neural Information Processing Systems 30 (NeurIPS 2017), pp. 5998–6008. arXiv:1706.03762. (아키텍처: §3; scaled dot-product 및 multi-head attention: §3.2; positional encoding: §3.5; 복잡도 비교: §4, Table 1; 학습 설정: §5.1–5.4; 결과: §6, Table 2.)" },
  { id: 2, text: "Bahdanau, D., Cho, K., & Bengio, Y. (2015). Neural Machine Translation by Jointly Learning to Align and Translate. In 3rd International Conference on Learning Representations (ICLR 2015). arXiv:1409.0473. (RNN encoder-decoder 에 additive attention 을 도입한 원 연구.)" },
  { id: 3, text: "Ba, J. L., Kiros, J. R., & Hinton, G. E. (2016). Layer Normalization. arXiv:1607.06450." },
  { id: 4, text: "Alammar, J. (2018). The Illustrated Transformer. https://jalammar.github.io/illustrated-transformer/ (self-attention 의 6단계 분해, multi-head, positional encoding, residual/LayerNorm 을 그림으로 설명. 'it' coreference 예시와 attention 시각화 인용.)" },
  { id: 5, text: "Rush, A. M. et al. (2018, updated 2022 by Huang, A., Subramanian, S., et al.). The Annotated Transformer. Harvard NLP. https://nlp.seas.harvard.edu/annotated-transformer/ (논문을 줄 단위 PyTorch 구현으로 옮긴 튜토리얼. √dₖ 스케일링의 분산 논증, LR schedule·label smoothing 구현 인용.)" },
  { id: 6, text: "Weng, L. (2018). Attention? Attention! Lil'Log. https://lilianweng.github.io/posts/2018-06-24-attention/ (attention 을 '중요도 가중치 벡터'로 정의하고 additive/dot-product·self/cross·soft/hard 로 분류.)" },
  { id: 7, text: "Bloem, P. (2019). Transformers from Scratch. https://peterbloem.nl/blog/transformers (self-attention 을 가중 평균에서부터 1차 원리로 재구성. 내적을 '관련도'로 보는 영화 추천 직관, √k 스케일링 인용.)" },
  { id: 8, text: "Devlin, J., Chang, M.-W., Lee, K., & Toutanova, K. (2019). BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding. In Proceedings of NAACL-HLT 2019, pp. 4171–4186. arXiv:1810.04805." },
]} />
