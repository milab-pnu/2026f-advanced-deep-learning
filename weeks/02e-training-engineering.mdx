---
title: "Training engineering"
week: 2
order: 4
---

모델 크기와 학습 토큰 수를 정해 계산량을 추산했어도, 실제 학습 완료 시간은 아직 정해지지 않는다. GPU(graphics processing unit)의 메모리에 무엇을 저장하고, 장치 사이에 무엇을 전달하는지가 실행 가능성과 속도를 결정한다. Duan 등의 분산 LLM(large language model) 학습 서베이[^survey]를 바탕으로, 메모리 부족에서 출발해 병렬화와 실제 실행 구성을 연결한다.

## 1. 학습 자원

### 1-1. 부동소수점과 정밀도

지금까지 가중치와 activation을 실수로 썼지만, 컴퓨터는 무한한 자릿수를 저장할 수 없다. **Bit는 0 또는 1 한 자리, byte는 8 bits**다. FP32는 수 하나에 32 bits, 즉 4 bytes를 쓰는 부동소수점(floating-point) 형식이다. FP16과 BF16(bfloat16)은 각각 16 bits, 즉 2 bytes를 쓴다. Token ID라는 정수로 임베딩을 찾는 것과, 찾은 임베딩의 각 실숫값을 저장하는 것은 다른 단계다.

부동소수점은 과학적 표기 $1.234 \times 10^5$처럼 **자릿수와 크기를 나누어** 담되, 이진수를 사용한다. 부호는 양수·음수를, 지수(exponent)는 표현 범위(dynamic range)를, 유효숫자의 소수부(fraction)는 같은 크기에서 얼마나 촘촘히 값을 구분하는지, 즉 정밀도(precision)를 정한다.[^bf16]

| 형식 | 부호 bits | 지수 bits | 소수부 bits | 수 하나의 크기 |
|---|---|---|---|---|
| FP32 | 1 | 8 | 23 | 4 bytes |
| FP16 | 1 | 5 | 10 | 2 bytes |
| BF16 | 1 | 8 | 7 | 2 bytes |

표의 소수부는 실제 저장하는 bits다. 정규화된 이진수에서는 맨 앞의 1을 생략해 저장하므로 유효숫자는 이보다 한 bit 많다. 여기서는 비트 패턴을 외우기보다 **같은 16 bits를 어디에 배분했는가**를 보자.

- **FP16**은 FP32보다 지수도 소수부도 줄였다. 최대 유한값은 65,504다. 계산 결과가 범위를 넘으면 overflow가, 너무 작으면 0으로 반올림되는 underflow가 생길 수 있다.
- **BF16**은 FP32와 지수 bits가 같아 표현 범위가 비슷하다. 대신 FP16보다 소수부가 짧아 같은 크기에서 구분할 수 있는 값의 간격이 넓다. 넓은 범위가 곧 높은 정밀도는 아니다.

**작은 업데이트가 사라지는 예**를 보자. 1 이상의 가까운 수 사이 간격은 FP16에서 $2^{-10} \approx 0.000977$, BF16에서 $2^{-7}=0.0078125$다. 정확한 결과가 $1+0.001=1.001$인 덧셈을 한 번 한 뒤 가장 가까운 표현값으로 반올림(rounding)하면:

| 저장 형식 | 저장되는 결과 |
|---|---|
| FP32 | 약 1.00100005 |
| FP16 | 1.0009765625 |
| BF16 | 1.0 |

BF16으로 가중치를 저장하면서 매번 작은 업데이트를 바로 더하면 변화가 사라질 수 있다. 그래서 **mixed precision**은 모든 값을 낮은 정밀도로 바꾸는 대신, 연산과 저장 항목에 맞게 정밀도를 섞는다. 예를 들어 행렬곱은 BF16 입력으로 수행하되 누적은 FP32로 하고, optimizer의 갱신용 가중치(master weight)와 상태를 FP32로 유지한다. 이때 작은 변화는 master weight에 누적되고, 계산에 쓸 때 낮은 정밀도로 변환한다.[^mixed]

FP16 학습에서는 작은 gradient가 0이 되는 것을 줄이기 위해 **loss scaling**도 사용한다. Loss에 큰 상수를 곱해 역전파(backpropagation)하고, optimizer 갱신 전 gradient에서 그 배율을 되돌리는 방식이다. BF16에서는 넓은 범위 덕분에 보통 필요하지 않지만, 반올림 오차나 불안정한 학습까지 없어지는 것은 아니다.[^amp]

이제 “8B 모델이 몇 GB인가”는 **파라미터 수뿐 아니라 무엇을 어떤 형식으로 저장하는가**의 문제다. 낮은 정밀도는 저장·전송량을 줄이고 지원 하드웨어에서 연산을 빠르게 할 수 있지만, dtype과 실제 속도는 구현에 따라 확인해야 한다. 추론에서 가중치·KV cache를 더 적은 bits로 저장하는 양자화(quantization)는 [6주차](/lecture/2026f-advanced-deep-learning/06a-llm-inference/#4-정밀도와-저장-비용)에서 이어간다.

### 1-2. 메모리 구성

8B 파라미터를 각각 2 bytes로 저장하면 16 GB다. 이 모델을 24 GB GPU에서 학습할 수 있을까? 아직 계산에 넣지 않은 항목이 있다. Backward를 위해 보관하는 중간 활성값(activation), 가중치마다 생기는 gradient, optimizer가 유지하는 상태를 저장해야 한다.

다음은 bf16(bfloat16) 가중치·gradient, fp32(32-bit floating point) master weight와 Adam의 두 상태를 사용하는 **예시 구성**이다. 구현마다 dtype과 master weight 유지 여부가 달라져 실제 사용량은 달라진다. GB는 $10^9$ bytes 기준이다.

| 저장 항목 | 파라미터당 bytes | 8B에서의 크기 |
|---|---|---|
| bf16 가중치 | 2 | 16 GB |
| bf16 gradient | 2 | 16 GB |
| fp32 master weight | 4 | 32 GB |
| Adam의 1차·2차 모멘트 | 8 | 64 GB |
| 합계 | 16 | 128 GB |

Activation과 임시 통신 buffer는 아직 더하지 않았다. ZeRO(zero redundancy optimizer) 논문은 이러한 model state의 중복을 메모리 문제의 주요 원인으로 분석한다.[^zero]

따라서 문제를 둘로 나눈다. **학습 상태가 메모리에 들어가게 하는 것**, 그리고 **주어진 시간 안에 충분한 토큰을 처리하는 것**이다. GPU를 여러 개 쓴다는 말만으로 두 문제가 모두 해결되지는 않는다.

### 1-3. Sequence packing

GPU에 모델이 들어가더라도, 계산하는 입력의 상당 부분이 빈칸이라면 자원을 낭비할 수 있다. 길이 8의 입력 배열을 사용하는 단순한 예제를 보자. 문서 A의 입력 길이는 3, B는 4다. 각각을 길이 8에 맞추면 남는 위치를 **padding**으로 채운다.

아래 A0·A1·A2는 A의 입력 토큰 위치, B0–B3는 B의 입력 토큰 위치다. `·`는 padding이며 실제 학습 텍스트가 아니다.

| 구성 | 길이 8의 입력 배열 | 유효 입력 위치 |
|---|---|---|
| 개별 padding: A | `A0 A1 A2 ·  ·  ·  ·  ·` | 3 / 8 |
| 개별 padding: B | `B0 B1 B2 B3 ·  ·  ·  ·` | 4 / 8 |
| Sequence packing: A+B | `A0 A1 A2 B0 B1 B2 B3 ·` | 7 / 8 |

개별 padding은 총 16칸 중 7칸(43.75%)이 유효하고, packing은 8칸 중 7칸(87.5%)이 유효하다. **Sequence packing은 여러 짧은 시퀀스를 하나의 배열에 함께 담아 padding을 줄이는 방법**이다. 단순한 dense 연산에서는 loss에서 padding을 무시해도 입력 배열의 빈 위치에 대한 연산이 남을 수 있어, 빈칸 자체를 줄이는 것이 중요하다.[^packing]

함께 담는다고 두 문서가 한 문맥이 되어야 하는 것은 아니다. 독립 문서를 유지하려면 A는 A 안에서만, B는 B 안에서만 이전 위치를 참조하게 한다. 예를 들어 B0는 앞에 저장된 A0–A2를 보지 못하고, B2는 B0·B1·B2만 본다. 실제 구현은 시퀀스 경계 정보를 attention 연산에 전달한다. 입력을 단순히 이어 붙이는 것만으로 이 조건이 충족되지는 않는다.

정답도 [Pre-training의 문서별 입력·정답 예제](/lecture/2026f-advanced-deep-learning/02c-pre-training/#3-4-학습-시퀀스와-문서-경계)처럼 각 문서에 맞춘 값을 유지한다. 입력 배열을 채운 정도가 두 배가 되었다고 학습 속도가 반드시 두 배가 되는 것은 아니다. Attention 구현, 배열 구성 비용, batch 설정에 따라 실제 이득이 달라진다. 같은 학습 목표와 유효 토큰 수를 기준으로 처리량(throughput), 즉 단위 시간에 처리한 유효 토큰 수를 비교해야 한다.

이 예제는 padding이 많이 생기는 조건을 가정했다. Batch 안의 최대 길이까지만 채우거나 비슷한 길이를 묶는 방법도 padding을 줄일 수 있다. 이미 문서들을 이어서 고정 길이를 채우는 pre-training은 같은 낭비가 작을 수 있다. Megatron Bridge의 packed-sequence 기능은 주로 길이가 다양한 fine-tuning 예제를 위한 것으로 설명된다. 따라서 이름만 보고 모든 pre-training에 켜야 하는 옵션으로 받아들이지 않는다.[^packing]

### 1-4. 장치 간 통신

메모리를 나누어 저장하면 필요한 값을 주고받아야 한다. 이후 병렬화 설명에서 사용할 통신 연산을 먼저 구분하자. 다음 표에서 reduce는 합산을 뜻한다. 평균 gradient가 필요하면 합산 결과를 적절한 수로 나누는 과정도 포함한다.

| 통신 연산 | 네 장치가 하는 일의 개념 |
|---|---|
| All-reduce | 각자의 값을 합산하고 합산 결과를 모두가 받는다. |
| All-gather | 각자 가진 조각을 모아 모두가 전체를 받는다. |
| Reduce-scatter | 값을 합산한 뒤 결과의 서로 다른 조각을 나누어 받는다. |
| All-to-all | 각 장치가 목적지별로 다른 조각을 보내고, 다른 장치들에서 자기 몫을 받는다. |

통신 시간에는 메시지를 보내기 시작하는 지연(latency)과 데이터를 전송하는 시간이 함께 들어간다. 작은 통신을 너무 잘게 나누면 지연이 누적되고, 큰 tensor를 자주 보내면 대역폭(bandwidth)이 중요해진다. 그래서 같은 GPU 수라도 장치 연결과 통신 빈도가 다르면 처리량이 달라진다.[^survey]

## 2. 데이터·상태 분산

### 2-1. DP

가장 먼저 생각할 방법은 같은 모델을 GPU마다 복제하고 서로 다른 데이터를 읽히는 것이다. 이를 DP(data parallelism)라고 한다. PyTorch의 DDP(distributed data parallel)는 이 방식의 대표적인 구현이다.[^ddp]

```
GPU 0: 같은 모델 θ + 배치 A → gradient g₀
GPU 1: 같은 모델 θ + 배치 B → gradient g₁
                         ↓ 평균
                두 GPU 모두 같은 갱신 적용
```

유효 토큰 수가 같은 배치라면 $g=(g_0+g_1)/2$로 평균하면 된다. 배치마다 채점하는 토큰 수가 다르면 token 단위 평균을 맞추도록 가중해야 한다. 평균한 gradient로 같은 optimizer step을 수행하면 모델 복제본이 일치한다.

DP의 장점은 서로 다른 입력을 동시에 처리하는 것이다. 하지만 단순 복제에서는 GPU마다 전체 가중치와 optimizer 상태를 유지하므로, **모델 하나의 메모리가 부족한 문제는 그대로**다. 또한 backward 과정에서 gradient를 주고받는 시간이 든다.

### 2-2. Batch와 gradient 누적

한 번에 큰 배치를 넣기 어렵다면 작은 microbatch 여러 개의 gradient를 누적(gradient accumulation)한 뒤 optimizer를 한 번 갱신할 수 있다.

$$
B_{\text{global}}=B_{\text{micro}}\times G_{\text{DP}}\times A
$$

$G_{\text{DP}}$는 DP replica 수, $A$는 accumulation 횟수다. 각 microbatch가 같은 길이와 유효 토큰 수를 갖는다고 가정한다. GPU당 2개 시퀀스, DP 4개, accumulation 8회라면 한 번의 갱신에 64개 시퀀스를 사용한다. 길이가 2,048이면 입력 위치는 총 131,072개이며, loss에서 제외한 위치가 있다면 유효 정답 토큰 수는 더 작다.

TP나 PP로 같은 모델 계산을 나누는 GPU 수까지 DP 수에 곱하면 batch를 과대 계산한다. Gradient accumulation은 batch를 나누어 처리하는 방법이며, 큰 batch 자체가 항상 더 좋은 학습을 보장하는 것도 아니다.

### 2-3. ZeRO와 FSDP

DP에서 모든 GPU가 optimizer 상태를 똑같이 갖고 있어야 할까? 업데이트할 파라미터 구간을 나누면 각 GPU는 자기 구간의 optimizer 상태만 유지할 수 있다. ZeRO는 이 생각을 단계적으로 확장한다.[^zero][^deepspeed]

| 방식 | DP 그룹 안에서 나누어 저장하는 것 |
|---|---|
| ZeRO-1 | optimizer state |
| ZeRO-2 | optimizer state + gradient |
| ZeRO-3 | optimizer state + gradient + parameter |

앞의 128 GB 예제에서 fp32 master weight와 두 모멘트 96 GB를 optimizer 쪽 상태로 묶자. GPU 4개에 나눈 model state의 이상적인 저장량은 다음과 같다.

| 방식 | GPU당 계산 | GPU당 크기 |
|---|---|---|
| 단순 DP | $16+16+96$ | 128 GB |
| ZeRO-1 | $16+16+96/4$ | 56 GB |
| ZeRO-2 | $16+(16+96)/4$ | 44 GB |
| ZeRO-3 | $(16+16+96)/4$ | 32 GB |

이것은 상시 저장하는 model state의 단순 모형이다. 실제 peak memory에는 연산 직전에 모으는 parameter, activation, 통신 buffer, allocator 여유 공간 등이 추가된다. ZeRO-3를 사용한다고 32 GB GPU에 반드시 들어간다는 계산은 아니다.

FSDP(fully sharded data parallel)도 학습 상태를 분산 저장한다. PyTorch FSDP2에서는 필요한 parameter를 연산 전에 all-gather로 모으고, backward의 gradient를 reduce-scatter로 합산·분할한다. 메모리를 줄이는 대신 필요한 시점에 통신해야 한다.[^fsdp]

FSDP와 ZeRO-3는 비슷한 메모리 절감 목표를 갖지만 API와 저장·통신 단위, 지원 옵션이 같은 구현은 아니다. 실제 선택에서는 사용하려는 모델과 프레임워크의 지원 범위를 확인한다.

## 3. 모델·시퀀스 분산

### 3-1. TP

TP(tensor parallelism)는 하나의 층 안에서 큰 행렬 연산을 나눈다. Megatron-LM은 Transformer의 attention과 FFN(feed-forward network)에 이 방식을 적용한다.[^megatron]

예를 들어 $Y=XW$에서 $X$가 $2\times4$, $W$가 $4\times6$이라고 하자. $W$를 열 방향으로 두 조각으로 나누면 각각 $4\times3$이 된다.

$$
W=[W_0\;W_1],\qquad Y=[XW_0\;XW_1]
$$

| GPU | 입력 | 가중치 | 계산한 출력 |
|---|---|---|---|
| 0 | 같은 $X$ | $W_0$ | $Y_0=XW_0$, 크기 $2\times3$ |
| 1 | 같은 $X$ | $W_1$ | $Y_1=XW_1$, 크기 $2\times3$ |

전체 출력이 필요하면 두 조각을 이어 붙인다. 다음 연산도 분할된 형태로 수행할 수 있다면 매번 전체를 모을 필요는 없다.

<Details summary="보충: 행 방향 분할의 행렬식">

반대로 행 방향으로 나눈 가중치와 그에 맞춰 나눈 입력은 부분 결과를 **더해야** 한다.

$$
X=[X_0\;X_1],\qquad
W=\begin{bmatrix}W_0\\W_1\end{bmatrix},\qquad
XW=X_0W_0+X_1W_1
$$

</Details>

TP는 행렬 연산의 규모를 나누지만 층마다 필요한 통신이 생긴다. 같은 batch를 사용하므로 TP 장치를 두 배 늘린다고 서로 다른 학습 샘플을 두 배 읽는 것은 아니다. 빠른 장치 간 연결이 중요한 이유다.

### 3-2. PP

PP(pipeline parallelism)는 층을 여러 stage로 나눈다. 32층 모델을 두 GPU로 나누면 GPU 0이 1–16층, GPU 1이 17–32층을 맡는 식이다. 앞 stage의 출력 activation은 뒤 stage의 입력이 되고, backward에서는 gradient가 반대 방향으로 이동한다.[^gpipe]

배치 하나만 넣으면 GPU 1은 GPU 0이 끝날 때까지 기다린다. 여러 microbatch를 흘려보내면 일부 작업을 겹칠 수 있다. 아래는 forward만 그린 교육용 시간표이며, 각 stage의 실행 시간이 같다고 가정한다.

| 시간 | GPU 0: 앞 절반 | GPU 1: 뒤 절반 |
|---|---|---|
| 1 | microbatch A | 대기 |
| 2 | microbatch B | microbatch A |
| 3 | microbatch C | microbatch B |
| 4 | 대기 | microbatch C |

처음과 마지막의 빈 구간이 pipeline bubble이다. 실제 학습에서는 backward를 언제 배치하는지, activation을 얼마나 보관하는지까지 scheduling해야 한다. Stage마다 층 수가 같아도 embedding·출력 head 등의 비용으로 실행 시간이 다를 수 있다.

따라서 PP에서는 “나누었는가?”와 함께 **stage 사이의 작업량이 균형적인가?**를 확인한다.

### 3-3. SP와 CP

§1-3의 sequence packing은 여러 짧은 입력을 함께 담는 방법이다. 여기서 다루는 SP·CP는 시퀀스 방향의 계산이나 저장을 여러 장치에 나누는 방법이다. 모델 가중치를 나누어도 시퀀스가 길어지면 activation이 커진다. 이때 시퀀스 방향으로 나누는 방법이 필요하다. 문헌과 구현마다 용어가 다를 수 있으므로 여기서는 Megatron의 구분을 따른다.[^parallel]

**SP(sequence parallelism)**는 TP와 함께 사용해, LayerNorm 같은 일부 연산에서 TP 장치마다 중복되던 activation을 시퀀스 방향으로 분할한다. 예를 들어 길이 8인 activation을 두 장치가 길이 4씩 보유하게 하는 식이다. 다음 TP 연산에 맞춰 gather 또는 scatter를 적용한다. Activation 저장량을 줄이는 것이 주요 동기다.[^sp]

**CP(context parallelism)**는 입력 시퀀스를 나누어 attention을 포함한 층 전반에서 처리한다. 예를 들어 앞 4토큰과 뒤 4토큰을 나누어 맡는다. 뒤쪽 query는 causal mask상 앞쪽 key/value도 참조해야 하므로 장치 사이에 필요한 정보를 전달해야 한다.[^parallel]

```
GPU 0: 위치 0–3의 query 처리
GPU 1: 위치 4–7의 query 처리 ← 앞 위치의 key/value도 필요
```

<Callout type="warning">
긴 문서를 두 조각으로 자르고 서로 통신하지 않으면, 뒤 조각은 앞 문맥을 읽을 수 없다. 그것은 원래의 긴 context attention과 다른 계산이다. CP는 필요한 문맥을 교환하면서 작업을 나누는 방법이다.
</Callout>

### 3-4. EP

MoE(mixture of experts)는 여러 expert 중 토큰별로 일부를 선택해 계산하는 구조다. EP(expert parallelism)는 이 expert들을 여러 장치에 나누어 배치한다.[^switch]

예를 들어 expert 0은 GPU 0, expert 1은 GPU 1에 있다고 하자. Router가 GPU 0의 어떤 토큰을 expert 1로 보내기로 했다면 그 토큰의 activation을 GPU 1로 전달해야 한다. 계산이 끝나면 출력도 원래 토큰 순서에 맞게 돌려놓는다.

여러 장치가 서로 다른 토큰 묶음을 주고받는 all-to-all 통신과 expert별 부하 균형(load balancing)이 중요해진다. 많은 토큰이 하나의 expert에 몰리면 다른 장치가 기다릴 수 있다. 이번 주에는 이 분산 문제까지만 이해하고, router·expert 구조와 학습 방법은 다음 주에 다룬다.

## 4. 분산 학습 구성

### 4-1. 병렬화 조합

이 노트에서 본 DP·TP·PP·SP·CP·EP는 여섯 개의 독립적인 스위치를 모두 켜라는 목록이 아니다. 예를 들어 Megatron의 SP는 TP 그룹과 함께 사용하고 EP는 MoE가 있을 때 의미가 있다.[^parallel]

<Figure src="https://arxiv.org/html/2407.20018v1/fig_3d_parallelism.svg" alt="Data parallelism, tensor parallelism, pipeline parallelism을 함께 구성한 Duan 등의 3D parallelism 도식." caption="DP는 데이터 배치, TP는 층 내부 계산, PP는 층의 구간을 나눈다. 같은 GPU에 세 역할이 함께 지정될 수 있다." source="Duan et al. (2024), Efficient Training of Large Language Models on Distributed Infrastructures: A Survey, Figure 8. CC BY 4.0, 원본 변경 없음." hero />

[원본 Figure 8](https://arxiv.org/html/2407.20018v1#S4.F8)의 각 축을 데이터·층 내부·층 구간으로 읽으면 병렬화의 조합을 이해하기 쉽다. 그림은 논문에 명시된 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)으로 재사용한다. 원도식의 Sequence Parallel 표기는 §3-3에서 설명한 Megatron SP와 동일한 구현으로 읽지 않는다. 문헌에 따라 시퀀스 분할의 명칭과 범위가 다르다. 다음은 DP·TP·PP를 장치 번호로 풀어 쓴 별도의 16 GPU 예제다.

Dense 모델을 16개 GPU로 학습하며 TP=2, PP=2, DP=4를 적용한다고 하자. 모델 하나의 계산을 담당하는 묶음은 TP와 PP를 합쳐 4개 GPU이고, 이 묶음이 서로 다른 데이터를 읽는 replica 4개로 구성된다.

$$
16=2_{\text{TP}}\times2_{\text{PP}}\times4_{\text{DP}}
$$

| 서로 다른 배치를 읽는 replica | 앞쪽 층의 TP 쌍 | 뒤쪽 층의 TP 쌍 |
|---|---|---|
| 0 | GPU 0·1 | GPU 2·3 |
| 1 | GPU 4·5 | GPU 6·7 |
| 2 | GPU 8·9 | GPU 10·11 |
| 3 | GPU 12·13 | GPU 14·15 |

한 행의 네 GPU가 하나의 모델 계산을 함께 수행한다. 예를 들어 GPU 0·1은 TP 통신을 하고 앞 stage의 출력을 GPU 2·3에 전달한다. GPU 0·4·8·12는 같은 파라미터 조각을 맡는 DP 그룹이므로 서로 gradient를 동기화한다. 이 표의 번호 배치는 개념 설명용이며 실제 장치 배치는 연결 속도를 고려해 정한다.

CP까지 사용하면 이 예제의 장치 수 계산에 CP 차원도 포함한다. SP는 별도 독립 장치 수를 곱하는 항이 아니다. EP의 그룹 구성은 프레임워크와 MoE 설정을 함께 봐야 하므로 여섯 값을 무조건 곱하지 않는다.

| 막힌 지점 | 먼저 살펴볼 선택 |
|---|---|
| 모델은 들어가지만 처리량이 부족 | DP와 batch 구성, 데이터 공급 속도 |
| 복제된 model state가 너무 큼 | FSDP·ZeRO |
| 한 층의 연산·가중치가 큼 | TP |
| 층 전체를 한 장치가 맡기 어려움 | PP와 stage 균형 |
| 긴 시퀀스의 activation이 큼 | SP·CP·activation 재계산 |
| MoE expert가 많음 | EP와 token routing 부하 |

이 표는 출발점이다. 최종 구성은 하드웨어 연결, 시퀀스 길이, 모델 구조와 실제 profiling으로 결정한다.

### 4-2. 메모리와 처리량 최적화

**Activation checkpointing.** Backward에 필요한 중간 결과를 모두 저장하는 대신 일부만 저장하고 나머지는 재계산한다. 저장 공간과 추가 연산을 교환하는 방법이다. 장애 복구를 위해 파일에 저장하는 checkpoint와는 다른 의미다.[^checkpoint]

**통신과 계산의 겹치기.** 한 층의 gradient를 전달하는 동안 앞 층의 backward를 계산할 수 있다면 대기 시간을 줄일 수 있다. 하지만 의존성이 남는 통신은 숨길 수 없고, 장치 간 연결의 속도도 중요하다.

GPU 수 $G$에서 처리량을 $T_G$라고 하면 scaling efficiency를 $T_G/(G T_1)$로 살펴볼 수 있다. 단, 한 GPU에서도 같은 모델이 실행되어야 이 기준이 의미가 있다. 그렇지 않으면 실행 가능한 최소 장치 수를 기준으로 비교한다. 모델·시퀀스 길이·정밀도 등 비교 조건을 함께 기록한다.

### 4-3. 도구와 recipe

| 도구·프로젝트 | 읽을 때 확인할 역할 |
|---|---|
| PyTorch DDP | 모델 복제와 gradient 동기화 |
| PyTorch FSDP2 | PyTorch 모델의 학습 상태 sharding |
| DeepSpeed | ZeRO 및 분산 학습·메모리 최적화 기능 |
| Megatron Core / Megatron Bridge | Transformer 병렬화와 학습 설정·모델 연결 |
| NVIDIA NeMo | 대규모 모델 학습·사용자화의 프레임워크 생태계 |
| Nemotron | 모델·데이터·공개 training recipe를 읽을 실제 사례 |

FSDP2의 예제는 [공식 튜토리얼](https://docs.pytorch.org/tutorials/intermediate/FSDP_tutorial.html), ZeRO 단계별 설정은 [DeepSpeed 문서](https://www.deepspeed.ai/tutorials/zero/), 병렬화 조합은 [Megatron Bridge 가이드](https://docs.nvidia.com/nemo/megatron-bridge/latest/parallelisms.html)에서 확인한다.[^fsdp][^deepspeed][^parallel] API는 버전에 따라 바뀌므로 설정 파일을 옮길 때 문서 버전과 설치 버전을 맞춘다.

Nemotron은 FSDP와 동일한 종류의 병렬화 API 이름이 아니다. [공개 training recipe](https://docs.nvidia.com/nemotron/latest/index.html)를 이용하면 모델 구성·데이터·학습 단계가 실제 실행 설정으로 연결되는 모습을 볼 수 있다.[^nemotron] 공개 recipe의 데이터와 전체 공개 모델을 학습한 데이터가 완전히 같은지도 별도로 확인한다.

### 4-4. 측정과 장애 복구

빠른 한 step만으로 며칠짜리 학습의 완료 시간을 설명할 수는 없다. 데이터 로딩 대기, 평가, checkpoint 저장, 장치 장애와 재시작이 포함된다. 서베이는 분산 학습의 reliability도 별도의 주제로 다룬다.[^survey]

실행을 재개하려면 가중치 외에 optimizer·학습률 scheduler·random state·데이터 진행 위치를 어떻게 복원할지도 정해야 한다. 학습 로그에는 loss뿐 아니라 유효 tokens/s, peak memory, 통신 대기와 재시작 이력을 남긴다. 시스템을 바꾼 뒤 속도는 올랐지만 처리하는 데이터나 loss 정규화가 달라졌다면 같은 학습의 가속이라고 보기 어렵다.

[^survey]: Duan, J., et al. (2024). *Efficient Training of Large Language Models on Distributed Infrastructures: A Survey*. [arXiv:2407.20018v1](https://arxiv.org/abs/2407.20018v1). — 대표 서베이·지정 읽기: §IV의 병렬화, §V의 연산, §VI의 메모리, §VII의 통신, §VIII의 reliability.
[^zero]: Rajbhandari, S., Rasley, J., Ruwase, O., & He, Y. (2020). *ZeRO: Memory Optimizations Toward Training Trillion Parameter Models*. SC 2020. [arXiv:1910.02054](https://arxiv.org/abs/1910.02054). — model state 메모리 분해와 세 단계 partitioning. 본문 8B·4 GPU 수치는 명시한 dtype 가정으로 별도 계산.
[^ddp]: PyTorch Contributors. (2026). *DistributedDataParallel*. [공식 문서](https://docs.pytorch.org/docs/2.8/generated/torch.nn.parallel.DistributedDataParallel.html). — replica와 gradient 동기화, 입력 분할의 책임. 접근: 2026-09-14.
[^deepspeed]: DeepSpeed Team. (2026). *Zero Redundancy Optimizer*. [공식 튜토리얼](https://www.deepspeed.ai/tutorials/zero/). — ZeRO stages 1–3의 상태 분산. 접근: 2026-09-14.
[^fsdp]: PyTorch Contributors. (2026). *Getting Started with Fully Sharded Data Parallel (FSDP2)*. [공식 튜토리얼](https://docs.pytorch.org/tutorials/intermediate/FSDP_tutorial.html). — parameter all-gather와 gradient reduce-scatter, fully_shard. 접근: 2026-09-14.
[^megatron]: Shoeybi, M., et al. (2019). *Megatron-LM: Training Multi-Billion Parameter Language Models Using Model Parallelism*. [arXiv:1909.08053](https://arxiv.org/abs/1909.08053). — §3, Transformer 내부의 tensor model parallelism. 본문의 작은 행렬 예제는 자체 구성.
[^gpipe]: Huang, Y., et al. (2019). *GPipe: Efficient Training of Giant Neural Networks using Pipeline Parallelism*. NeurIPS 2019. [arXiv:1811.06965](https://arxiv.org/abs/1811.06965). — microbatch와 pipeline scheduling. 본문의 forward 시간표는 두 stage의 교육용 예제.
[^parallel]: NVIDIA. (2026). *Parallelisms Guide*. [Megatron Bridge 공식 문서](https://docs.nvidia.com/nemo/megatron-bridge/latest/parallelisms.html). — SP·CP의 구분, 병렬화 조합과 DP size 계산. 접근: 2026-09-14.
[^sp]: Korthikanti, V. A., et al. (2023). *Reducing Activation Recomputation in Large Transformer Models*. MLSys 2023. [arXiv:2205.05198](https://arxiv.org/abs/2205.05198). — sequence parallelism과 selective activation recomputation.
[^switch]: Fedus, W., Zoph, B., & Shazeer, N. (2022). *Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity*. Journal of Machine Learning Research, 23(120), pp. 1–39. [arXiv:2101.03961](https://arxiv.org/abs/2101.03961). — token routing, expert 분산과 부하 균형.
[^checkpoint]: PyTorch Contributors. (2026). *torch.utils.checkpoint*. [공식 문서](https://docs.pytorch.org/docs/2.8/checkpoint.html). — activation을 저장하는 대신 backward에서 재계산하는 방식. 접근: 2026-09-14.
[^nemotron]: NVIDIA. (2026). *Nemotron Training Recipes*. [공식 문서](https://docs.nvidia.com/nemotron/latest/index.html), [Stage 0: Pretraining](https://docs.nvidia.com/nemotron/latest/nemotron/nano3/pretrain.html). — 모델·데이터·학습 recipe의 공개 범위. 접근: 2026-09-14.

[^mixed]: Micikevicius, P., et al. (2018). *Mixed Precision Training*. ICLR 2018. [arXiv:1710.03740](https://arxiv.org/abs/1710.03740). — 낮은 정밀도 연산과 높은 정밀도 상태를 조합하는 학습의 기본 원리. 원논문은 fp16 중심이며 본문의 bf16 구성은 메모리 계산을 위한 별도 가정.

[^packing]: NVIDIA. (2026). *Packed Sequences*. [Megatron Bridge 공식 문서](https://docs.nvidia.com/nemo/megatron-bridge/latest/training/packed-sequences.html). — What It Is, When to Use It: padding 절감, 시퀀스 경계 보존, 일반 pre-training과의 구분. 접근: 2026-09-14. 3·4토큰을 길이 8 배열에 담은 수치는 본문을 위한 교육용 계산.

[^bf16]: Kalamkar, D., et al. (2019). A Study of BFLOAT16 for Deep Learning Training. [arXiv:1905.12322](https://arxiv.org/abs/1905.12322). — FP32·FP16·BF16의 비트 배분과 BF16 학습.

[^amp]: PyTorch. (2025, v2.8). [Automatic Mixed Precision](https://docs.pytorch.org/docs/2.8/amp.html). — 연산별 dtype 선택과 gradient scaling.
